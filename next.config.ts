import type { NextConfig } from 'next';

const nextConfig: NextConfig = {
  /* config options here */
  // @ts-ignore - Next.js internal option
  allowedDevOrigins: ['seismic-dab-stable.ngrok-free.dev'],
};


export default nextConfig;
